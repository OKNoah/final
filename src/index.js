import Component from './Component'
import createServer from './createServer'
import reduxConnect from './reduxConnect'
import hoist from './hoist'

const Final = {
  Component, createServer, hoist, reduxConnect
}

export {
  Component, createServer, hoist, reduxConnect
}

export default Final
